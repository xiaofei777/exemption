当时摄像头是 30fps，如果每一帧都直接上传，带宽和后端处理压力都比较大。但实际使用中，很多连续帧几乎没有变化，所以我没有简单地提高服务器配置，而是把问题前移到浏览器端解决。

具体来说，我先把视频缩放到 160×120，降低计算量；然后取相邻两帧的 RGB 数据，计算每个颜色通道的平均绝对差值。如果平均差值低于阈值，就认为画面基本没有变化，直接丢弃；只有超过阈值的关键帧才进行 JPEG 压缩并上传。

这里的工程难点不只是实现公式，而是阈值和性能之间的平衡。阈值太低，灯光闪烁会造成误触发；阈值太高，又可能漏掉人的正常移动。我通过实际场景测试选择了 30 左右的阈值，并且只比较 RGB，在保证检测效果的同时减少计算量。