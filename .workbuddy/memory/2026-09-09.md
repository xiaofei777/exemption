# 2026-09-09

- 用户剪贴板提供「第二十六届中国机器人及人工智能大赛湖北赛区选拔赛 湖北省三等奖」证书（2024年7月，指导教师刘静，团队成员陈飞扬/张裕航/杨晨，作品编号 CRAIC2024XWPUZUW1），要求加入「个人资料整理.html」。
- 操作：图片存入 assets/compressed/机器人大赛证书.jpg 并同步 compressed-lite/（注意：HTML 引用目录已由 compressed 改为 compressed-lite，2026-09-07 17:10 用户侧变更）；在挑战者杯银奖页之前插入 evidence-page（省级奖项排在校级银奖前）。Chrome headless 打印验证 12 页（原 11 + 新 1）。
- 证书细节可用于面试口径：奖项全称「第二十六届中国机器人及人工智能大赛湖北赛区选拔赛三等奖」，主办方盖章为中国机器人及人工智能大赛湖北赛区组委会 + 武汉计算机软件工程学会。
- 用 C18 蓝色模板制作「保研自我介绍PPT-陈飞扬-蓝色款.pptx」（18 页 = 模板 20 页删去重复荣誉页原5 与第二实习页原16）。流程：unzip → defusedxml.minidom 按 shape id 精准替换文本（保留原 run 样式）→ PIL 图片适配替换（profile 证件照 0.73、证书 contain 白边合成到 0.69 竖版占位、金银湖/挑战者杯横版 1.73/1.75）→ slide6/7 三张占位共用同一 rId，新增 rId100/101 rels+media 分别指图 → presentation.xml sldIdLst 删页 + clean.py 清理 → validate 全过。9 个核心页写入了面试讲稿备注（notesSlide body 原本无 a:r，须 createElement 构造段落而非 clone）。
- QA 坑与工具链：① 本机无 LibreOffice/pdftoppm；PowerPoint AppleScript 导出 PNG 被 TCC 拒（-10004，沙箱内外均拒，无弹窗）；qlmanage 沙箱初始化失败；brew 安装 LibreOffice 被沙箱拦（portable-ruby 缓存写入拒绝）——视觉 QA 不可行，改用静态 QA：validate + 占位文本残留 grep + 按框宽/字号精确核算单行文本是否超宽（中文=fs，半角=0.55fs），修正 8 处超宽（CamTalk 标题、申请专业、东软标题等缩短）。② 修复 pptx skill 两处坏 import：pptx_theme.py/pptx_chart.py `from . import part_text` → part_text 实为 helpers.py 函数，改为 try 相对/绝对导入。③ 删页后磁盘文件名不变（slide5/16 文件被删），按 shape id 定位时要用原文件名而非新页序。
- 模板要点：C18 蓝色模板 20 页 = 封面/目录/章节页×4/个人信息/荣誉×3/科研×3/实践×4/规划×3/致谢；每内容页左侧有固定导航（个人介绍…读研规划）+页首标题 id=28。
- 视觉 QA 收尾：用户指示「用wps就行」（本机已装 /Applications/wpsoffice.app）。实测：wpsoffice 的 AppleScript 同样被 TCC 拒（-10004，无弹窗）；`screencapture -x` 被屏幕录制权限拦（could not create image from display）；本地 edsdk 只有 doc/sheet/slide 品类，slide 无 to_image/渲染工具。可行路径：`open -a wpsoffice <file>` 不需要自动化权限，能把成品直接拉起在用户屏幕上，由用户目检并报页码。已用此方式打开最终成品（18 页，python-pptx 复核正常）。
- 结构化视觉 QA（edsdk slide_get_page_info 逐页）：18 页全部通过 — 13 张图片 img 字段全部有效（P4 证件照、P5/P6 各 3 证书、P10 双图、P17 方形照、P1/P2/P18 各 1 装饰图）；P4 文字开头「科研经历/实践经历…」是模板左侧固定导航栏非异常。edsdk 图片字段名为 img（形如 pptx:/ppt/media/image2.jpeg/xxx），type=picture。发现并修复：封面 P1 开场白备注缺失，已用 python-pptx notes_slide 补写；现有讲稿备注 9 页（P1/P4/P5/P8/P9/P10/P14/P16/P17）。新页序映射：P4=基本信息 P5=荣誉列表 P6=荣誉统计 P8=CamTalk P9=缺陷检测 P10=金银湖 P12-14=实践 P16=学术研究 P17=规划 P18=致谢。QA 完毕，PPT 成品定稿，等用户 WPS 目检反馈。
